import React from "react"
import useAxios from "../../../useHooks/useAxios"

export default function GroupSelection() {
  const url = `http://localhost:3001/BattleGroups/Teams/`

  return <div></div>
}
